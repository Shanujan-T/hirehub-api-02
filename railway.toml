[build]
builder = "nixpacks"

[deploy]
startCommand = "PYTHONPATH=. python migrations/run_all.py && gunicorn run:app --bind 0.0.0.0:$PORT --workers 1 --timeout 120"
healthcheckPath = "/"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
